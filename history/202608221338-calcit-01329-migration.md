# qrcode-tetris Calcit 0.13.29 migration

- Migrated the project from `compact.cirru` to canonical `calcit.cirru` and current `calcit`/`caps` workflow commands.
- Updated the Phlox dependency to the validated `codex/fix-get-env-calcit-01329` branch while the shared fix is under review.
- Replaced legacy dynamic field access with Option-safe `get`/`option:unwrap-or` access and fixed the current `update-states` arity.
- Narrowed the nullable grid-size and promise boundaries and corrected Option-returning `nth`/`first` uses.
- Updated Vite to 5.x and Pixi to 7.x, and added the runtime dependencies required by current Phlox codegen.
- Verified with Calcit 0.13.29: check-only passed, JS codegen passed, immutable Yarn install passed, and Vite production build passed.
